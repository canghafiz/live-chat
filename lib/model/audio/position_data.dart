class PositionData {
  final Duration position, bufferedPosition;
  final Duration? duration;

  PositionData({
    required this.position,
    required this.bufferedPosition,
    required this.duration,
  });
}
